# Example Codex Task

Update this Remotion project to create a 45-second beginner-friendly video explaining what a terminal is.

Use `examples/generated-video-plan.json` as the planning reference.

Requirements:

- Update the scene data in `src/videoPlan.ts`.
- Keep the visual style clean and educational.
- Make text large enough for video but short enough to fit.
- Use clear scene transitions.
- Run `npm run typecheck`.
- Summarize what changed.

